# PORTING / 設計と計画

## 方針

- SAS Viyaの「一気通貫のアナリティクス・ライフサイクル(読込→クレンジング→探索→集計→予測→可視化)」を、
  Rust製のオープンソースとして段階的に実装する。
- SAS社のコード・API・言語仕様(SASコード、CAS API)の複製はしない。機能の方向性のみを参考にする。
- コアは外部クレートに依存しない(サプライチェーンを小さく保つ)。

## フェーズ

| フェーズ | 内容 | 状態 |
|---|---|---|
| P0 | 列指向DataFrame・CSV入出力・型推論・クレンジング・describe・group by・相関・単回帰・CLI | ✅ 2026-09-24 |
| P1 | Parquet/JSON入力、日付型、結合(join)、ピボット、複数キーgroup by | 未着手 |
| P2 | 重回帰・ロジスティック回帰・決定木・k-means、モデルの保存/読込 | 未着手 |
| P3 | Web UI(ノーコードの可視化・ダッシュボード)、realdata.pro で公開 | 未着手 |
| P4 | Python(PyO3)/R バインディング | 未着手 |
| P5 | 並列化(マルチスレッド)→ 複数ノード分散実行 | 未着手 |

## 現在の実装メモ(P0)

- `Column` は型ごとの `Vec<Option<T>>`(int/float/bool/str)。`None` が欠損。
- CSVの型推論は int → float → bool → str の順に、欠損以外の全値が変換できるかで判定する。
  欠損とみなす値: 空欄 / `NA` / `N/A` / `null` / `NULL` / `NaN`。
- 数値列を平均値・中央値で補完すると、その列は float になる。
- フィルタでは欠損値の行を常に除外する(SQLのNULL比較と同じ考え方)。
- 並べ替えは安定ソートで、昇順では欠損値が先頭に来る。
